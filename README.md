# leonai-guide-assets

Image host for LeonAI Help Hub guide screenshots (referenced from the Zite
database via `raw.githubusercontent.com` URLs, pinned to a commit SHA).

**Do not delete files — URLs are live** and referenced from production
records. Folder names under `assets/` are opaque IDs by design; the
human-readable guide slug lives only in Zite, not here.
